# endpoint is localhost:8090/App
# user: viju
# pass: data2

# NOTE: the application endpont exposed is the same as the name of the ".war" file geneerated, the enpoint exposed is same that is project-name in build.properties file
